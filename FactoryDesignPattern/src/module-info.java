module FactoryDesignPattern {
	/* Basic Overview:
	 * Creational Design Pattern
	 * Used when the instance type depends on user input
	 * Provides Abstraction bt implementation and client classes
	 * */
	
	/* Implementation:
	 * Super class can be : Interface or Abstract Class or Class
	 * Factory class has a static method which returns the instance of SubClass
	 * */
}