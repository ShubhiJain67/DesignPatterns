module FacadeDesignPattern {
	/*
	 * Structural Design Pattern
	 * Used when we have multiple interfaces doing same work
	 * Facade interface helps client in routing.
	 * */
	
	/*
	 * Provides Abstract view of subsystems
	 * Used to define an entry point to all the sussytems
	 * There can be many sussystems interacting with each other in  complex manner s.
	 * If the become very complexy so e introduce a facade object which will get result from the subsytems and returns result to the user
	 * Subsystems are unawareof the faceds but they return the result to facade
	 * */
}